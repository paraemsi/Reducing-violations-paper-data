/*
 * Intentionally left blank.
 * All required definitions for the template-based circular buffer
 * are provided in circular_buffer.h so that the container can be
 * used header-only.  This translation unit merely satisfies build
 * systems that expect a corresponding .cpp file.
 */
