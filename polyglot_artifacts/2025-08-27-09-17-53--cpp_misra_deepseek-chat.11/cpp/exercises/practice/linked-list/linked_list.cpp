#include "linked_list.h"

namespace linked_list {
    // The implementation is now entirely in the header
    // Explicit template instantiations can be placed here if needed
    // But they're not necessary since the implementation is in the header
}  // namespace linked_list
