#include "complex_numbers.h"

namespace complex_numbers {

/* Everything is implemented inline in the header to enable constexpr and
 * allow the compiler to inline small functions efficiently, while keeping
 * the translation unit minimal.  This source file is therefore intentionally
 * left empty.  It exists to satisfy build systems that expect a .cpp file.
 */

}  // namespace complex_numbers
