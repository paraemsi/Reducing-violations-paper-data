#include "sublist.h"

namespace sublist {

// Instantiation helper – relies on the header‐only template implementation.
// Nothing else required here, but translation unit kept for build systems
// expecting a .cpp file.
}  // namespace sublist
