#include "all_your_base.h"

namespace all_your_base
{
    /* This translation unit is intentionally empty.
     * All functionality resides in the header in order to keep the
     * convert<>() template definition available to every translation unit
     * that includes it, while also avoiding duplicate symbol errors at link
     * time.
     */
} // namespace all_your_base
