#include "circular_buffer.h"

// All template-code is defined in the header, so this translation unit
// is intentionally left empty.  Keeping the file prevents linker issues
// when the project expects a corresponding object file.
namespace circular_buffer {

}  // namespace circular_buffer
