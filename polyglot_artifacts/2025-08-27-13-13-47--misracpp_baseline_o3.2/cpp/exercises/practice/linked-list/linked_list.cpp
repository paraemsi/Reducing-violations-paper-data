/*
 * Implementation note:
 * List<T> is a fully-templated class defined completely in the header
 * file (linked_list.h).  This translation unit is intentionally left
 * blank to satisfy build systems that expect a linked_list.cpp source
 * file, while preventing multiple template instantiations.
 */
