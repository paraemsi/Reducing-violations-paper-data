#include "sublist.h"

//  The algorithm is now implemented as a header-only, type-agnostic template.
//  This translation unit is intentionally left empty so that an object file
//  is still produced when the project builds.
