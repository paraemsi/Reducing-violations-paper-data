#include "circular_buffer.h"

// Implementation note:
// circular_buffer is a header-only template. This translation unit is
// intentionally minimal to satisfy build systems that expect a .cpp file.

namespace circular_buffer {

// no non-template definitions

}  // namespace circular_buffer
