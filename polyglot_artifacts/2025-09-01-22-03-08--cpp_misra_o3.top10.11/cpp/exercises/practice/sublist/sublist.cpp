#include "sublist.h"

/*
 * The comparison logic is fully defined in the header to keep the template
 * implementation inline.  This translation unit exists only to satisfy build
 * systems that expect a corresponding .cpp file.
 */
