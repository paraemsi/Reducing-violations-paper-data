#include "sublist.h"

namespace sublist
{
    /* All functionality is implemented in the header
       via templates and inline functions to avoid
       multiple definition issues. */
}  // namespace sublist
