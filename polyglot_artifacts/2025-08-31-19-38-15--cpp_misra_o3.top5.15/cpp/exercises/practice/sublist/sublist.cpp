#include "sublist.h"

namespace sublist
{
    /* Intentionally left empty.
     * All template definitions are provided in the header to allow
     * header-only usage without violating the One Definition Rule.
     */
}  // namespace sublist
