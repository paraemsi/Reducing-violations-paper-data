#include "circular_buffer.h"

// Implementation is header-only; this TU exists to satisfy build systems and
// to provide a stable place for potential future non-template code.

namespace circular_buffer {

}  // namespace circular_buffer
