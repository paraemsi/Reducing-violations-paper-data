#pragma once

#include <string>
#include <vector>

namespace diamond {

[[nodiscard]] std::vector<std::string> rows(char letter);
[[nodiscard]] std::string make(char letter);

}  // namespace diamond
