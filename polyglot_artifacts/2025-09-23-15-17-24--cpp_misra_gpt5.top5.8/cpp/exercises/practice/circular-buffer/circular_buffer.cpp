#include "circular_buffer.h"

namespace circular_buffer {
// Template implementations are provided inline in the header (to avoid link-time issues).
// This translation unit intentionally contains no additional code.
}  // namespace circular_buffer
