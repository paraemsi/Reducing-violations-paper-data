#include "sublist.h"

/*  Implementation file intentionally kept empty because all public
    functionality is provided as header-only inline templates in
    sublist.h.  Having this cpp ensures the translation unit exists in
    the build, while avoiding multiple-definition errors.  */
namespace sublist
{
} // namespace sublist
